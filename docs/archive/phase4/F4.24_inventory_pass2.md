# F4.24 Architecture Catalogue — PASS 2: End-of-Tier-2 Diagnostic Checkpoint

> **STATUS: DISPOSABLE DIAGNOSTIC (2026-06-10 snapshot).** This is the **F4.24a end-of-Tier-2
> checkpoint**, reported as a **DELTA against `F4.24_inventory_pass1.md`** (2026-06-04), NOT a
> standalone re-print. It verifies the three Tier-2 category drains landed (spirits/F4.20,
> consumables/D-F4-CONSUMABLES-TIER2, hexagrams/D-F4-HEXAGRAMS-TIER2) and seeds the Tier-3 worklist.
> Consume-and-discard; the living reference is the terminal F4.24b `ARCHITECTURE.md`. Structure it so
> the end-of-Tier-3 checkpoint can diff against THIS.

---

## Headline verdict

**Tier-2 accomplished its thesis** ("each subsystem owns its concerns; scattered logic drained to
canonical homes") **— complete modulo the known, explicitly-recorded Tier-3 inheritances.** Across all
three categories: **0 regressions** (nothing a DECISIONS_LOG record claims drained is still
mis-resident). The seepage that remains is either intentional Bucket-B/T/N (documented formula-terms,
structural state-machines, named-task hand-offs) or known-deferred to Tier-3 (X1, F4.38 + its stamp
sibling). The backbone shrank as predicted: **−2 files, −527 lines net**, with managers shedding lines
and effect-registries gaining them — the Phase-4 progress metric, visible.

**Seepage delta counts:**
| Category | Drained this era | Remains-by-design | Regression | Known-deferred → Tier-3 |
|---|---|---|---|---|
| Spirit (vs pass-1 §7) | 13 | ~28 (Bucket B/T/N + deferred state-machines) | **0** | sym_ducks, bullseye, symbiosis/algae (named hand-offs) |
| Consumable (fresh) | apply-surface fully landed | RunManager economy primitives (canonical) | **0** | Wu Xing proc surface (F4.38) + stamp runtime-trigger (F4.38-sibling) |
| Hexagram (fresh) | (was already clean) | 3-dispatch-class + accepted cycle + class-(c) reaches | **0** | X1 threefold scoring-loop dup |

---

## §3 — Backbone re-gen (delta vs pass-1)

### 3.1 File manifest delta

**Net: 36 files / ~19,509 lines → 34 files / 18,982 lines (−2 files, −527 lines).**

**Deleted (−2, confirmed gone):**
- `src/data/zodiacConsumables.js` (was 17) — folded into `consumables.js` (consumable block).
- `src/data/stamps.js` (was 145) — folded into `consumables.js` (consumable block).

**Notable line-count shifts (the drain signature):**
| File | pass-1 | pass-2 | Δ | Cause |
|---|--:|--:|--:|---|
| `RunManager.js` | 1862 | 1566 | **−296** | consumable apply-logic (applyElement/applyStamp/applyChakra) drained to ConsumableEffects |
| `ShrineScene.js` | 2022 | 1447 | **−575** | shop/forge logic drained; pickers + delegation cleanup (consumable block + Candidate G) |
| `GameScene.js` | 3963 | 3923 | −40 | net (some consumable dispatch simplified) |
| `ScoringEngine.js` | 109 | 104 | −5 | SNOW_MULT/ICE_MULT removed (F4.34) |
| `ConsumableEffects.js` | 426 | 715 | **+289** | absorbed apply-mutation bodies for all families |
| `consumables.js` | 137 | 295 | **+158** | absorbed zodiac + stamp static defs |
| `SpiritEffects.js` | 1267 | 1384 | **+117** | new migration hooks (F4.17/F4.18b/F4.20) |
| `HexagramEffects.js` | 767 | 831 | +64 | B1 header reconciliation (3-dispatch-class doc) |
| `GameRoundManager.js` | 2165 | 2198 | +33 | hook dispatchers added (net of inline removals) |
| `cards.js` | 912 | 931 | +19 | (incidental) |

The managers/scenes (`RunManager`, `ShrineScene`) shed ~870 lines; the effect-registries/data
(`ConsumableEffects`, `consumables`, `SpiritEffects`) gained ~560. That asymmetry IS the consolidation.

### 3.2 Hook tables delta

**Hexagram hooks — pass-1's 14 discrepancies → 0 (B1 resolved all).**
- `modifyInitialFlow` documented-but-dead: **DELETED** (B1). Confirmed zero hits in `src/`.
- 13 called-but-undocumented (`modifyPlaysPerTurn`, `shouldSpiritsFireTwice`, `revealsDeckFlip`,
  `discardUnmatchedDeckFlip`, `forceAutoBankOnYaku`, `disablesYaku`, `disableCaptureScoring`,
  `scoreFieldAtRoundEnd`, `modifyDeckFlipsPerTurn`, `onCaptureComplete`, `onPushSuccess`,
  `onPushFailure`, `onBank`): **all now documented** in the `HexagramEffects.js:7–93` header, grouped
  into the 3 dispatch classes (applyHook value-transformers/flags; direct `getActiveEffect()`
  side-effect/merge/array; Wu Xing `getX()` getters). The hook ROSTER (which effect defines which) is
  unchanged — B1 was documentation-only. Canonical table lives in the archived
  `hexagram_inventory_pass1.md` §2.

**Spirit hooks — roster GREW (the migration surface).** New hooks added since pass-1 by the
F4.17/F4.18b/F4.20 waves, each draining an inline GRM counter/effect into SpiritEffects.js:
`onCardPlayed` (sym_ants), `onWoodSlotCreated` + `onSilkAntiStrand` (engine_moths t1/t2),
`onFieldDiscard` (econ_recycling, engine_ship), `onBank` (engine_lincoln), `onPushSuccess`
(econ_reward), `onPushFailure` (engine_napoleon), `onRoundEnd` (sym_crow, decay_persimmon/pear),
`onRoundEndUnplayed` (sym_snails), `onStackCaptured` (engine_missing_number), `onCaptureComplete`
(util_glory). Dispatchers: `_fireCardPlayedHooks`, `_fireWoodSlotCreatedHooks`,
`_fireFieldDiscardHooks`, `_fireRoundEndUnplayedHooks`, `_fireStackCapturedHooks` (all GRM).

### 3.3 Dependency graph delta

**Edges removed** (the two deleted data files): `GameScene`, `ShrineScene`, `RunManager`,
`SpiritEffects` no longer import `stamps.js`; `ShrineScene`, `RunManager` no longer import
`zodiacConsumables.js`.
**Edges added:** `SpiritEffects.js` now imports `consumables.js` (data, was `stamps.js`);
`ConsumableEffects.js` now imports `cards.js` + `consumables.js` + `GameplayLogger` (grew its surface
as it absorbed the apply-bodies).

**Import cycles — same 3 as pass-1, now one formally accepted:**
1. `RunManager ↔ HexagramEffects` — **FORMALLY ACCEPTED** (D-F4-HEXAGRAMS-TIER2 Ruling 2; documented at
   the `HexagramEffects.js` import line). The only irreducible edge: `getActiveEffect`/`applyHook` need
   `run.getHexagram()`. Deliberately contained, not unresolved.
2. `RunManager ↔ SpiritEffects` — unchanged (RunManager imports `NEGATIVE_SNAPSHOT`/`SpiritEffects.get`;
   SpiritEffects imports `run`/`effectivePower`).
3. Transitive `ScoringEngine → SpiritEffects → RunManager → HexagramEffects → RunManager` — unchanged.
Cycles 2 & 3 are owned by the **GRM/RunManager destination audit (D-F4-SCOPE Part 2, late Tier-3)** —
not this checkpoint's to resolve.

---

## §4 — 3-category seepage diff (the core deliverable)

### 4.1 Spirit seepage (diff vs pass-1 §7) — **0 regressions**

Every pass-1 §7 entry re-verified against current source. **13 DRAINED** (inline logic gone, present as
a SpiritEffects hook — or, for `game_catcher`, as the intended interception GATE):
`util_glory`, `econ_reward`, `sym_ants`, `engine_moths` (both t1/t2), `sym_crow` (both bank+natural
paths de-duped), `sym_snails`, `engine_missing_number`, `econ_recycling`, `engine_ship` (GRM +
ConsumableEffects/Horse), `engine_lincoln`, `engine_napoleon`, `decay_persimmon`, `decay_pear`.

**~28 REMAIN-BY-DESIGN**, none a miss — each is a recorded Bucket-B/T/N keep or a named-task deferral:
- **Bucket-B formula/structural (kept, marked):** `econ_coupon` (`RunManager.js:351` getEffectiveCost),
  `econ_piggybank` (`:1228`), `econ_bonds`/`econ_ingot` (`:1256`), `legend_gankyil`
  autoCaptureThreshold (`GRM:403`, `FieldManager:44`), `legend_waidan` Grove-exit (`ShrineScene:981`),
  wood Leaf/Silk field bypass (`FieldManager:184/248/321`).
- **Bucket-T scoring-pipeline globals (correctly in the loop, not hooks):** `capstone_yinyang`/
  `universe`/`nature` (`GRM:1350-1352`, `:1496/1500/1601`), `capstone_time` flow+push (`RunManager:1122`,
  `:1539`), `engine_northern_lion` (`GRM:2067`).
- **Bucket-N / Wu Xing-adjacent (second-wave / F4.38 territory):** `engine_glacier`/`carbon`/`fossil`
  tier trackers (`GRM:825/839/895`), `engine_velocity` meteorite (`GRM:1375`), `econ_lucky_charm`
  (`RNGHook:14`, already canonical), `engine_palace` (`RunManager:1358/1453`), `engine_wildlife`/
  `plenty` (`RunManager:548`), `sym_caterpillar` (`GRM:1703`).
- **Deferred state-machines (no hook seam):** `sym_ducks` strand/deck-flip (`GRM:1917/1971`),
  `engine_bullseye` (`GRM:2092`), `sym_osprey` reset+interception (`GRM:375/1857`), `engine_golden_toad`
  per-capture reset (`GRM:1342`, loop-machinery; the gilding effect is a hook).
- **Structural state-init (acquisition machinery, never a migration target):** `_initSpiritState` /
  `ACCUMULATOR_INIT` (`RunManager:43/500`).
- **Named-task hand-offs:** `util_past_life` + `sym_cuckoo_egg` maturity/copy logic (RunManager +
  GameScene) → **F4.27**; `util_symbiosis`/`sym_algae` summon (`GRM:1728`) → recorded "do LAST".

**INTRODUCED-by-Tier-2 spirit seepage: none.** The discard/round-end pipelines consolidated *toward*
hooks, reducing seepage.

### 4.2 Consumable seepage (fresh — no pass-1 baseline) — **0 mis-residencies**

Enumerated against the `D-F4-CONSUMABLES-TIER2` canonical-home rule. The apply-surface for every family
is landed in `ConsumableEffects.js`: `element_*` (`:657-702`), `stamp_*` apply (`:607-623`), `chakra_*`
(`:465-600`), `zodiac_*` (`:41-260`). `RunManager.applyElement`/`applyStamp`/`applyChakra` are **gone**
(only economy primitives + doc-comments remain at `RunManager:1466-1497`). Scenes are clean delegators
(`GameScene:2310`, `ShrineScene:1209` build params → `ConsumableEffects.get(id).execute(...)`). Three
Marks (`mark_*`) and Four Practices/Paramita are **fully deleted** (zero hits). RunManager's
`buyConsumable`/`sellConsumable`/`generateRandomConsumable` are the canonical economy API, **not**
seepage.

**Known-deferred (NOT seepage):**
- **Wu Xing proc surface (F4.38 → Tier-3):** Water `getWaterMult`/`depLevel++`, Fire break, Earth
  interest, Wood scoring/slots woven into the scoring loop (`GRM:463/821/891/1393/1544`). Intentionally
  deferred whole.
- **Stamp runtime-trigger surface (NEW — F4.38-sibling):** the *proc half* of stamps (retrigger-on-score
  `GRM:1015`; fire ki/draw/consumable on discard `GRM:1124-1140`, capture `GRM:1679-1697`, yaku
  `GRM:2116-2132`) is structurally bound to the scoring/capture loop — itself a 3-way internal
  duplication mirroring F4.38's Fire/Water/Wood triad. The consumable block's A1 scope was the stamp
  *apply* half only; this proc half was never claimed. **Recommend filing as an F4.38-sibling for the
  Tier-3 scoring-loop pass**, not a Tier-2 reopen. (Also: `PHASE4_consolidation_candidates.md:119`
  flags a live bug here — `_dispatchStampDiscardEffects` draws to hand with no cap check.)

### 4.3 Hexagram seepage (fresh) — **0 regressions; end-state confirmed**

Confirmed the documented `D-F4-HEXAGRAMS-TIER2` end-state: fully hook-shaped, **zero manager-resident
hex logic**, no `hex.effect ===` name-checks in the engine (recon H1). The 3-dispatch-class model holds;
the import cycle is accepted+documented; the class-(c) reaches (`one_yaku_disabled`,
`eight_spirits_graduated_tax`) are documented, not normalized (no [PRESERVE]-safe param swap exists).

**Known-deferred → Tier-3 (X1, NOT regression):** the threefold scoring-loop duplication is
**intentionally still present**, inseparable from the Tier-3 dedup that subsumes F4.38:
- `onCardScored` merge — `GRM:1437-1454` (`_addCapture`) & `GRM:483-495` (`_scoreFieldCards`).
- `computeFinalScore` — `GRM:1628-1631` & `GRM:539-542`.
- `onCaptureComplete` — `GRM:1787-1790` & `GRM:1326-1329` (disableCaptureScoring branch).
Exact sites preserved in archived `hexagram_inventory_pass1.md` §7.

---

## §5 — Observations + import-cycle diff (vs pass-1 §8)

**8 RESOLVED, 6 REMAIN** of the 14 pass-1 observations:

| # | pass-1 observation | Status | Evidence / note |
|---|---|---|---|
| 1 | `modifyInitialFlow` dead hook | **RESOLVED** | deleted (B1); zero hits |
| 2 | header 13 undocumented hooks | **RESOLVED** | `HexagramEffects.js:7-93`, 3-class doc |
| 3 | SNOW_MULT/ICE_MULT vestigial | **RESOLVED** | removed (F4.34); `getWaterMult` sole source |
| 4 | `test-run.js:54` crashes on `totalPoints` | **REMAINS** | still calls non-existent method; dead smoke-test |
| 5 | two `typeof window` debug blocks, one unguarded | **REMAINS** | `RunManager.js:1559` (ungated) + `:1565` (DEV) |
| 6 | `_trackSnailsUnplayed` dup'd across round-end paths | **RESOLVED** | → `onRoundEndUnplayed` hook; single `_endRound` |
| 7 | `econ_recycling`+`engine_ship` discard dup | **RESOLVED** | → `onFieldDiscard`; single `_discardCard` (`GRM:1268`) |
| 8 | `decay_*` decrement dup | **RESOLVED** | → `onRoundEnd` hook (F4.18b) |
| 9 | `sym_crow` gen dup | **RESOLVED** | → `onRoundEnd` hook; single path |
| 10 | `zodiac_rabbit` "Remove push penalty" sets `_dogProtection` | **REMAINS** | `ConsumableEffects.js:88-94`; cosmetic naming |
| 11 | ShrineScene dynamic `import()` of ConsumableEffects | **RESOLVED** | now static (`ShrineScene.js:21`); zero dynamic imports |
| 12 | `spiritTooltip.js` 30+ id-switch | **REMAINS** | ~79 `case`/`id===` occurrences; tooltipBase candidate (Candidate C-adjacent) |
| 13 | RunManager symbiont state-init parallel to `_initSpiritState` | **REMAINS** | `_initSpiritState` (`:500`) + `_initSpiritElements` (`:589`); `game_catcher` in both |
| 14 | GRM capstone flags via `.some()` per capture | **REMAINS** | `GRM:1350-1352`; uncached |

The 6 remainers are all **low-priority / non-blocking** (2 dead-code/cosmetic: #4, #10; 1 debug hygiene:
#5; 3 consolidation candidates: #12, #13, #14 — none Tier-2-scope; #13/#14 feed the late destination
audit, #12 feeds Candidate C).

**NEW observations (introduced/surfaced near Tier-2):**
- **N1 — `engine_northern_lion` is the lone surviving `run.activeSpirits` accumulator** (`GRM:2069`).
  The F4.20-FIX/FIX2 sweep moved every other counter `activeSpirits → allSpirits`; northern_lion was
  deliberately scoped out (shop-utility, not a scoring accumulator). Consistent with the triage, but
  worth a one-line **ratification** that excluding transcended copies from `pushesWitnessed` is intended.
- **N2 — stamp runtime-trigger 3-way duplication** (see §4.2) — F4.38-sibling for Tier-3.
- **N3 — `engine_lincoln` desc/behavior mismatch** (`SpiritEffects.js:1103`) — migrated faithfully under
  [PRESERVE]; desc says "bank without pushing" but increments on every bank. Reconcile separately
  (D-F4-DOCSYNC-style, balance-adjacent).

---

## §6 — Tier-2 verdict

**Tier-2 is complete, modulo the known Tier-3 inheritances.** The thesis held: each subsystem now owns
its concerns. Spirit effect-logic drained to `SpiritEffects.js` (13 migrations, 0 regressions);
consumable apply-logic drained to `ConsumableEffects.js` (full apply-surface, 0 mis-residencies, data
consolidated −2 files); hexagram behavior is fully hook-shaped (0 manager-resident logic). The backbone
shrank −2 files / −527 lines with the manager→registry asymmetry that proves the drain. No category
drain failed to land — every record's "drained" claim verifies.

**Nothing blocks declaring Tier-2 complete.** The remaining work is all explicitly-deferred and recorded:

**Worklist this snapshot seeds for Tier-3:**
1. **X1 — hexagram scoring-loop dedup** (onCardScored/computeFinalScore/onCaptureComplete twins) +
   **F4.38 — Wu Xing proc surface** + **N2 — stamp runtime-trigger surface** → the single Tier-3
   scoring-loop pass (all three are the same "proc behavior bound to the 3-cluster scoring loop" shape).
2. **Destination audit (D-F4-SCOPE Part 2)** — owns import cycles #2/#3 + the `_initSpiritState`/
   `_initSpiritElements` twins (obs #13) + capstone-flag caching (obs #14); feeds F4.24b.
3. **F4.27** — `util_past_life` + `sym_cuckoo_egg` maturity/copy logic (locked F2.5 design).
4. **Named spirit hand-offs** — `util_symbiosis`/`sym_algae` summon ("do LAST"); `sym_ducks`/`bullseye`
   state-machines (need deck-flip/rank-inventory seams).
5. **Ratify/cleanup (non-Tier-3):** N1 (northern_lion activeSpirits intent), N3 (lincoln desc),
   obs #4/#5/#10 (dead smoke-test, debug-block hygiene, zodiac_rabbit naming), Candidate C (obs #12
   tooltipBase). Plus the late F4.14/F4.24b doc sweep (D-F4-DOCSYNC).

**Next checkpoint:** end-of-Tier-3 diffs against THIS (pass-2). The success metric: the §4 seepage tables
and the §5 remainder both shrink further — particularly the §4.2/§4.3 "known-deferred" rows collapsing
as the Tier-3 scoring-loop pass lands.
